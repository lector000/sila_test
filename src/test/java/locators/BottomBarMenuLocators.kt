package locators

import construction_classes.LocatorsConstructor

class BottomBarMenuLocators {

    val ParofileButton = LocatorsConstructor(
        androidId = "ru.sportmaster.app.handh.dev:id/profile_graph"
    )
}